name: bump

# Point the formula at a new Navigator release.
#
# TWO TRIGGERS, ONE PATH. `repository_dispatch` is the automatic one:
# `neon-law-source-code/navigator`'s `deploy.yml` fires it after the CLI
# archives are attached to the GitHub Release, so a `brew upgrade` reaches the
# new version minutes after the tag lands with nobody in the loop.
# `workflow_dispatch` is the repair handle — re-run any tag by hand when a bump
# went wrong, without re-cutting a release in the other repository. A `YY.M.D`
# tag admits one release per UTC day, so there is no second chance over there.
#
# THE DIGESTS ARE COMPUTED HERE, FROM THE PUBLISHED BYTES. The dispatch carries
# a tag and nothing else — no digests, no URLs. That is deliberate: a payload
# carrying digests would let a malformed or spoofed dispatch pin this formula to
# bytes nobody verified, and it would also mean this workflow could not repair
# itself from a bare tag. `scripts/bump.sh` downloads exactly what it will tell
# users to download.
on:
  repository_dispatch:
    types: [navigator-release]
  workflow_dispatch:
    inputs:
      tag:
        description: "Navigator release tag to point the formula at (YY.M.D or YY.M.D-hotfix.N)"
        required: true
        type: string

# The formula is committed by this job. That is the whole grant: no packages,
# no id-token, and nothing that reaches the Navigator repository.
permissions:
  contents: write

concurrency:
  # Two releases can never share a UTC day, but a dispatch racing a manual
  # re-run can. Serialize so they cannot both push to `main`.
  group: bump-formula
  cancel-in-progress: false

jobs:
  bump:
    name: point the formula at a release
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v5

      # BEFORE `scripts/bump.sh`, not after it. Homebrew is NOT preinstalled on
      # the Ubuntu 24.04 image `ubuntu-latest` now resolves to, and the bump asks
      # `brew` itself whether the new tag sorts above the outgoing one — that is
      # the comparison deciding whether `version_scheme` has to move, and the
      # only honest way to answer it is to ask the program that will act on it.
      # With this step later in the job, the bump died on `brew: command not
      # found` instead.
      #
      # Pinned by SHA rather than `@master`: it configures the tool this job
      # trusts to verify a formula before that formula is published.
      - uses: Homebrew/actions/setup-homebrew@8f3d1ec8a696b3b9d9a6c3696b6c73033cab69e4

      - name: resolve the release tag
        id: release
        env:
          # Exactly one of these is set, decided by the trigger. Read both into
          # the environment rather than interpolating into the script body: a
          # `client_payload` is attacker-shaped input in the general case, and
          # it must never reach a shell as code.
          DISPATCH_TAG: ${{ github.event.client_payload.tag }}
          MANUAL_TAG: ${{ inputs.tag }}
        run: |
          set -euo pipefail
          tag="${DISPATCH_TAG:-${MANUAL_TAG:-}}"
          if [ -z "${tag}" ]; then
              echo "::error::neither client_payload.tag nor inputs.tag was set"
              exit 1
          fi
          # Shape-check before the value is used to compose a URL or a commit
          # message. `bump.sh` checks this too; doing it here means a malformed
          # dispatch fails on the line that read it.
          #
          # Both shapes Navigator publishes: an ordinary `YY.M.D` and a same-day
          # `YY.M.D-hotfix.N`. The formula follows whichever release is newest
          # rather than the newest of one shape — it holds a single version, so
          # skipping hotfixes leaves `brew` on whatever ordinary release last
          # succeeded, which is a stale formula nothing reports.
          if ! printf '%s' "${tag}" |
              grep -Eq '^[0-9]{2}\.(0|[1-9][0-9]?)\.(0|[1-9][0-9]?)(-hotfix\.(0|[1-9][0-9]*))?$'; then
              echo "::error::'${tag}' is not a YY.M.D or YY.M.D-hotfix.N release version"
              exit 1
          fi
          echo "tag=${tag}" >> "$GITHUB_OUTPUT"
          echo "bumping to ${tag}"

      - name: rewrite the formula
        env:
          TAG: ${{ steps.release.outputs.tag }}
        run: scripts/bump.sh "${TAG}"

      - name: show the change
        run: git --no-pager diff -- Formula/navigator.rb

      # Install the rewritten formula BEFORE committing it. This runner is
      # x86_64 Linux, which is one of the two prebuilt paths, so the check costs
      # one archive download and proves the digest matches the bytes GitHub
      # serves. A bad bump then fails here rather than on a reader's machine,
      # where it surfaces as a checksum mismatch they can do nothing about.
      #
      # It does not prove the macOS half — `test.yml` covers that after the
      # push, because no Linux runner can install a Mach-O.
      # `setup-homebrew` registers THIS repository as the tap, so there is no
      # `brew tap` to run — issuing one against the workspace path collides with
      # the registration it already made ("remote mismatch") and fails the job.
      #
      # What that leaves worth checking is which tree the qualified name now
      # resolves to. If the tap pointed at the GitHub remote rather than this
      # checkout, every job below would silently test `main` instead of the
      # commit under test, and a pull request could go green on a formula it
      # does not contain.
      - name: confirm the tap is this checkout
        run: |
          set -euo pipefail
          tap_path="$(brew --repository neon-law-source-code/navigator)"
          echo "tap resolves to ${tap_path}"
          resolved="$(cd "${tap_path}" && pwd -P)"
          workspace="$(cd "${GITHUB_WORKSPACE}" && pwd -P)"
          if [ "${resolved}" != "${workspace}" ]; then
              echo "::error::the tap resolves to ${resolved}, not this checkout (${workspace}) — CI would be testing a different tree than the one under review"
              exit 1
          fi
      # AUDIT WHAT IS ABOUT TO BE PUSHED, not just what installs. `test.yml`
      # audits the formula on every push and pull request, but it never sees a
      # bump: this job pushes with `GITHUB_TOKEN`, and GitHub does not start
      # workflows for pushes made with it. No `test.yml` run exists for any
      # bump commit, so until now the publishing path had no audit at all and a
      # formula could reach `main` in a state the gate would have refused. That
      # is how one did — `brew audit` rejects an explicit `version` on an
      # ordinary release, the first of which reached this tap at `26.8.24`, and
      # the failure surfaced days later on an unrelated pull request.
      #
      # Same flags as `test.yml`, deliberately. If the two ever disagree, a
      # bump passes here and `main` is red the moment anyone opens a pull
      # request against it — which is the failure this step exists to prevent,
      # reintroduced one level up.
      - name: audit, install, and test the formula on this runner
        run: |
          set -euo pipefail
          brew audit --online --except=version --formula neon-law-source-code/navigator/navigator
          brew install --formula neon-law-source-code/navigator/navigator
          brew test neon-law-source-code/navigator/navigator

      # THIS PUSHES STRAIGHT TO `main`, and `main` is deliberately unprotected.
      # A tap holds one mechanical file whose every change is a formula bump
      # this job already proved by installing it, so there is nothing for a
      # review gate to read and no second branch for the history to need: `main`
      # is a straight line of one commit per Navigator release, each naming the
      # tag it points the formula at. Do not add a ruleset here — a
      # `pull_request` or `required_signatures` rule refuses this push outright
      # (a runner's `git commit` is unsigned; only API and web commits are
      # auto-verified), and the formula then silently stops following releases.
      # `navigator ops github setup` applies its common review gate to any
      # repository it is pointed at, so it must not be pointed at this one.
      #
      # TWO THINGS THIS PUSH CANNOT INHERIT, both of which cost a failed release
      # to learn, and both of which fail at the very end of an otherwise green
      # job — after the formula is rewritten, installed, and tested.
      #
      # The REFSPEC is explicit because `actions/checkout` leaves `main` with no
      # tracking upstream: a bare `git push` exits 128 with "The current branch
      # main has no upstream branch".
      #
      # The CREDENTIAL is explicit because the checkout's persisted one does not
      # reach this step. `actions/checkout` does store it — it sets
      # `http.https://github.com/.extraheader` in the local config — but after
      # `setup-homebrew` has registered this workspace as a tap, the push no
      # longer picks it up and exits 128 with "could not read Username for
      # 'https://github.com'". Carrying the token in the URL is independent of
      # whichever of the two `setup-homebrew` moved, the header or the remote,
      # and it is not written to `.git/config`, so it does not persist into a
      # tree Homebrew reads. Actions masks the value in the log.
      - name: commit and push
        env:
          TAG: ${{ steps.release.outputs.tag }}
          # The job's own token. `permissions: contents: write` above is the
          # whole grant it carries, and this repository is the only thing it
          # can reach.
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          if git diff --quiet -- Formula/navigator.rb; then
              echo "formula already points at ${TAG} — nothing to commit"
              exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add Formula/navigator.rb
          git commit --message "navigator ${TAG}"
          git push "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git" HEAD:main
          echo "formula pushed at ${TAG}"
